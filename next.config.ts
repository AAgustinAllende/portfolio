import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["www.fundacionkonex.org", 
      "e7.pngegg.com",
      "static.vecteezy.com",
      "cdn3d.iconscout.com"
    ], // Agrega el dominio aquí
  },
};

export default nextConfig;
